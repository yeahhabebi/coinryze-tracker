services:
  - type: web
    name: coinryze-tracker
    env: python
    region: oregon
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: BOT_TOKEN
        value: 8320822050:AAGk4YmnvA5sqIWK5RcYodiCe9PNLp8bNUA
